# exp7 五级流水 CPU 设计记录

本文档记录从 exp6 单周期 CPU 迁移到 exp7 单发射五级流水 CPU 前需要处理的问题，以及建议的实现顺序。

## 一、当前基线中需要解决的问题

### 1. ALU 第一个操作数连接错误

`mycpu_top.v` 中已经生成了 `alu_src1` 和 `alu_src2`，但 ALU 实例的两个输入都连接到了 `alu_src2`：

```verilog
alu u_alu(
    .alu_op     (alu_op    ),
    .alu_src1   (alu_src2  ),
    .alu_src2   (alu_src2  ),
    .alu_result (alu_result)
);
```

应将 `alu_src1` 端口连接到 `alu_src1` 信号，否则双寄存器运算、地址计算及 PC 相对运算无法得到正确结果。

### 2. 调试写使能信号名称不一致

顶层端口声明为 `debug_wb_rf_we`，但当前赋值使用了 `debug_wb_rf_wen`。如果未关闭 Verilog 隐式线网，这会生成一个新的隐式信号，并导致真正的输出端口未被驱动。

流水化后应统一使用 `debug_wb_rf_we`，并从 WB 级有效指令产生：

```verilog
assign debug_wb_rf_we = {4{ws_valid && ws_gr_we}};
```

### 3. ALU 的 OR 运算存在组合自反馈

`alu.v` 中的 OR 运算包含 `alu_result`：

```verilog
assign or_result = alu_src1 | alu_src2 | alu_result;
```

这会形成组合逻辑环。应只计算两个输入的按位或：

```verilog
assign or_result = alu_src1 | alu_src2;
```

### 4. ALU 右移结果位宽和取位错误

当前代码使用：

```verilog
assign sr_result = sr64_result[30:0];
```

右侧只有 31 位，赋给 32 位结果时最高位会被补零，算术右移的符号位会丢失。应根据 64 位移位结构检查并选择正确的 32 位区间，通常应为：

```verilog
assign sr_result = sr64_result[31:0];
```

修改后需要分别测试 `srli.w` 和负数的 `srai.w`。

### 5. `bl` 的寄存器写使能需要确认

当前 `gr_we` 表达式将 `inst_bl` 排除：

```verilog
assign gr_we = ~inst_st_w & ~inst_beq & ~inst_bne & ~inst_b & ~inst_bl;
```

但 `bl` 应把返回地址 `PC + 4` 写入 1 号寄存器，因此 `bl` 通常需要使能寄存器写回。流水化前应结合实验指令定义确认，并保证 `dest=5'd1`、写回数据为 `PC+4`。

### 6. 未声明或无效的控制信号需要清理

当前存在声明后未赋值或未使用的信号，例如 `load_op`。流水化时应明确使用 `inst_ld_w` 还是单独生成 `load_op`，避免片选控制引用悬空信号。

建议在顶层加入：

```verilog
`default_nettype none
```

并在文件末尾恢复默认设置，以便在编译阶段发现拼写错误和未声明信号。

### 7. SRAM 接口必须适配 Block RAM 时序

exp6 的单周期设计基本按照组合读存储器组织数据通路，而 exp7 的验证环境使用 Block RAM。Block RAM 通常具有同步读延迟：本周期给出地址和片选，读数据在后续周期有效。

因此不能只在原组合路径中插入寄存器，还必须明确：

- 指令地址在哪一级发出；
- 指令数据在哪一级被接收；
- 数据 RAM 读地址在哪一级发出；
- `ld.w` 的返回数据在哪一级有效；
- RAM 返回数据与 PC、目的寄存器号和写回控制是否处于同一条指令。

### 8. SRAM 片选信号需要由流水级有效状态控制

目前 exp7 中两个片选信号常置为 1，可用于初步连通接口，但流水化后的推荐控制为：

- `inst_sram_en`：取指级确实需要发起取指时有效；
- `data_sram_en`：MEM 级存在有效的加载或存储指令时有效；
- `data_sram_we`：MEM 级存在有效存储指令时为 `4'b1111`，其他情况为 `4'b0000`。

所有会产生体系结构副作用的信号必须与对应流水级的 `valid` 相与。

### 9. 分支行为需要与“不处理控制相关”的测试约定一致

exp7 不实现流水线冲刷，测试程序通过插入 NOP 回避相关冲突。仍需确认测试程序是否也在分支后安排了足够的 NOP，以及分支在 ID 级还是 EX 级决定。

分支决定越晚，顺序路径上进入流水线的错误指令越多。若本实验完全依赖软件 NOP，这些位置必须没有寄存器写入或存储器写入等副作用。

### 10. 调试接口必须来自 WB 级

单周期 CPU 中 PC、目的寄存器和写回结果属于同一条指令。流水化后，如果调试 PC 来自 IF 或 ID，而写回信息来自 WB，测试平台会看到错位的提交信息。

以下信号必须全部来自 WB 级的同一条有效指令：

- `debug_wb_pc`；
- `debug_wb_rf_we`；
- `debug_wb_rf_wnum`；
- `debug_wb_rf_wdata`。

## 二、五级流水 CPU 的建议结构

流水线划分为：

```text
IF -> IF/ID -> ID -> ID/EX -> EX -> EX/MEM -> MEM -> MEM/WB -> WB
```

每一级设置独立的有效位：

```text
fs_valid -> ds_valid -> es_valid -> ms_valid -> ws_valid
```

复位时所有有效位清零。由于本实验暂不处理阻塞，各级通常每周期向后推进一次。

### IF 级

负责维护 PC、产生取指地址和驱动指令 SRAM 接口。需要向 ID 级传递取回的指令及其 PC。

### ID 级

负责指令译码、寄存器堆读取、立即数生成、分支条件比较和控制信号生成。译码产生的数据和控制必须同时写入 ID/EX 寄存器。

### EX 级

负责 ALU 运算、访存地址计算，并保存存储指令需要写入的数据。

### MEM 级

负责数据 SRAM 访问。加载指令取得存储器数据，存储指令产生片选、地址、四位写使能和写数据。

### WB 级

负责选择最终结果并写回寄存器堆，同时产生提交调试信息。

## 三、建议实现顺序

### 第 1 步：修正并验证单周期基线

先解决 ALU 输入、OR 运算、右移结果、`bl` 写回以及调试信号命名等问题。确保 exp6 的 20 条指令功能正确，再开始流水化。

完成判据：单周期测试通过，编译时没有端口宽度、隐式线网和未驱动输出警告。

### 第 2 步：确定各级边界和流水寄存器字段

先列出每一级需要使用的数据，不要边写代码边临时增加信号。重点保证 PC、控制信号和数据始终属于同一条指令。

建议的主要流水字段如下：

```text
IF/ID  : valid, pc, inst
ID/EX  : valid, pc, alu_op, alu_src1, alu_src2,
         store_data, dest, gr_we, mem_we, res_from_mem
EX/MEM : valid, pc, alu_result, store_data,
         dest, gr_we, mem_we, res_from_mem
MEM/WB : valid, pc, final_result, dest, gr_we
```

完成判据：每个后级组合逻辑只使用本级寄存器输出，不跨级直接引用前级临时信号。

### 第 3 步：实现有效位流水

为 IF、ID、EX、MEM、WB 建立 `valid`，复位时清零，正常情况下逐级传递。

完成判据：复位释放后，第一条指令按预期逐级到达 WB；流水线空泡不会写寄存器或存储器。

### 第 4 步：流水化 IF 和 ID

迁移 PC、取指和译码逻辑，处理 Block RAM 的同步取指时序。将指令及对应 PC 锁存在 IF/ID 寄存器中。

完成判据：连续取到的指令和 PC 一一对应，复位入口地址仍为 `0x1c000000`。

### 第 5 步：流水化 EX

将 ALU 控制、两个操作数及相关控制信号锁存到 ID/EX，再由 EX 级完成计算。

完成判据：无数据相关的 ALU 指令可以连续进入流水线，结果与单周期参考一致。

### 第 6 步：流水化 MEM

由 EX/MEM 寄存器驱动数据 SRAM，确保地址、写数据、写使能、片选和加载控制属于同一条指令。

完成判据：`ld.w`、`st.w` 单独测试通过，空泡及非访存指令不会触发数据 RAM 写入。

### 第 7 步：实现 WB 和调试接口

从 MEM/WB 寄存器取得写回信息，驱动寄存器堆写口和全部调试端口。

完成判据：测试平台看到的 PC、寄存器号和写回数据严格对应同一条提交指令。

### 第 8 步：接入分支反馈

根据选定的分支判定级产生 `br_taken` 和 `br_target`，反馈到取指 PC 选择器。exp7 暂不增加预测、冲刷和阻塞逻辑。

完成判据：在测试程序按要求插入 NOP 的前提下，`b`、`bl`、`beq`、`bne` 和 `jirl` 均可正确改变控制流。

### 第 9 步：按指令类别逐步回归

建议测试顺序为：

1. `add.w`、`addi.w` 等简单算术指令；
2. 逻辑、比较和移位指令；
3. `lu12i.w`；
4. `ld.w`、`st.w`；
5. 条件分支；
6. `b`、`bl`、`jirl`；
7. 最后运行插入 NOP 的 n1 至 n20 完整测试。

## 四、本阶段明确不实现的功能

exp7 暂不实现以下机制：

- 数据相关检测；
- 流水线阻塞；
- 数据前递；
- load-use 专用处理；
- 分支预测；
- 分支错误路径冲刷。

这些相关由测试程序插入 NOP 回避。设计时仍应保持清晰的流水级边界和 `valid` 控制，为 exp8 的阻塞、冲刷以及 exp9 的前递逻辑预留扩展基础。
